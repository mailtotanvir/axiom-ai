# Grafana dashboard provisioning (O5).
apiVersion: 1

providers:
  - name: Axiom dashboards
    orgId: 1
    folder: Axiom AI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
